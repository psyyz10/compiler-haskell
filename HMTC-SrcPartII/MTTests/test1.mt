// This is a comment.  It continues to the end of the line.
let
    var m: Integer;
    var a: Integer;
    const n: Integer = 9
in
    begin
        m := 1 + 2 * (n + 1);
        a := ((3 > 2) ? 3 : 2) + 1;
        putint(m)
    end
